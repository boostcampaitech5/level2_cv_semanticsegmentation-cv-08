import random
import torch
from smp.utils.util import *
import abc
from torch.utils.data import DataLoader

def main():
    train_loader = DataLoader(dataset=None, batch_size=1,shuffle=True,num_workers=0,drop_last=True,)

if __name__ == '__main__':
    main()